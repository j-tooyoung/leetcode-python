from typing import List

# 17. 电话号码的字母组合
class Solution:
    def letterCombinations(self, digits: str) -> List[str]:
        res = []
        def backtrack(digits, start):
            return
        backtrack(digits, 0)
        return res